-   call `parec` in python instead of piping…
-   put the playState listener in a separate thread
-   write an organizer to check for new sources and add listeners to them
-   …
-   **$$$ Profit $$$**
